<?xml version="1.0" encoding="UTF-8"?>
<msubsection id="import" title="import" xmlns:xi="http://www.w3.org/2001/XInclude">

  <p>Commit an unversioned file or tree into the repository.</p>
  <p>Recursively commit a copy of <code>path</code> to <code>url</code>.</p>
  <p>If <code>newEntry</code> is not set, copy top-level contents of <code>path</code> into 
  <code>url</code> directly.  
  Otherwise, create <code>newEntry</code> underneath <code>url</code> and begin copy there.</p>

  <attributes>
  
    <attribute name="path" required="true">
      <doc>source path to export from</doc>
    </attribute>

    <attribute name="url" required="true">
      <doc>source url to import to</doc>
    </attribute>

    <attribute name="newEntry" required="false">
    </attribute>

    <attribute name="message" required="true">
      <doc>commit message</doc>
    </attribute>

    <attribute name="recurse" required="false" default="true">
      <doc>Set to "false" to operate on single directory only.</doc>
    </attribute>
  
  </attributes>

</msubsection>