<?xml version="1.0" encoding="UTF-8"?>
<msubsection id="keywordsset" title="keywordsset" xmlns:xi="http://www.w3.org/2001/XInclude">

  <p>Keywordsset controls which keywords will be substituted on the given files. Valid keywords are:</p>
  <ul>
    <li>URL, HeadURL : The URL for the head version of the object.</li>
    <li>Author, LastChangedBy : The last person to modify the file.</li>
    <li>Date, LastChangedDate : The date/time the object was last modified.</li>
    <li>Rev, LastChangedRevision : The last revision the object changed.</li>
    <li>Id : A compressed summary of the previous.</li>
  </ul>

  <attributes>
  
    <grouped>
    
      <attribute name="file">
        <doc>File for which keywords will be substituted (specify nested filesets alternatively).</doc>
      </attribute>
    
      <attribute name="dir">
        <doc>All files in this directory will have their keywords substituted (recursively). 
        Specify nested filesets alternatively.</doc>
      </attribute>
    
    </grouped>
    
    <attribute name="keywords" required="false">
      <doc>The keywords to substitute on the given files.</doc>
    </attribute>
  
    <grouped>

      <attribute name="HeadURL">
        <doc>Set to "true" the keyword to substitute it on the given file.</doc>
      </attribute>
      
      <attribute name="URL">
        <doc>Set to "true" the keyword to substitute it on the given file.</doc>
      </attribute>

      <attribute name="Author">
        <doc>Set to "true" the keyword to substitute it on the given file.</doc>
      </attribute>
      
      <attribute name="LastChangedBy">
        <doc>Set to "true" the keyword to substitute it on the given file.</doc>
      </attribute>
    
      <attribute name="Date">
        <doc>Set to "true" the keyword to substitute it on the given file.</doc>
      </attribute>
    
      <attribute name="LastChangedDate">
        <doc>Set to "true" the keyword to substitute it on the given file.</doc>
      </attribute>
    
      <attribute name="Rev">
        <doc>Set to "true" the keyword to substitute it on the given file.</doc>
      </attribute>
    
      <attribute name="LastChangedRevision">
        <doc>Set to "true" the keyword to substitute it on the given file.</doc>
      </attribute>
    
      <attribute name="Id">
        <doc>Set to "true" the keyword to substitute it on the given file.</doc>
      </attribute>
    
    </grouped>
  
  </attributes>

  <p>Parameters specified as nested elements:</p>
  <ul>
    <li>fileset<br/>
    Filesets are used to select the files where the keywords will be set.</li>
    <li>dirset<br/>
    Dirsets are used to select the directories where the keywords will be set.</li>
  </ul>

</msubsection>