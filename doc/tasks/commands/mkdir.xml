<?xml version="1.0" encoding="UTF-8"?>
<msubsection id="mkdir" title="mkdir" xmlns:xi="http://www.w3.org/2001/XInclude">

  <p>Create a new directory under revision control.</p>
  <p>If target is a working copy path the directory is scheduled for addition in the working 
  copy.  If target is an url the directory is created in the repository via an immediate commit.</p>
  <p>In both cases all the intermediate directories must already exist.</p>

  <attributes>
  
    <grouped>
    
      <attribute name="path">
        <doc>path to create</doc>
      </attribute>
      
      <attribute name="url">
        <doc>url to create</doc>
      </attribute>
    
    </grouped>
  
    <attribute name="message" required="true">
      <doc>commit message</doc>
    </attribute>
  
  </attributes>

</msubsection>